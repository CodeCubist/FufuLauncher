<windowex:WindowEx
    x:Class="FufuLauncher.Views.DesktopNotificationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:FufuLauncher.Helpers"
    xmlns:windowex="using:WinUIEx"
    Title="FufuLauncher Notifications"
    Width="410"
    Height="180"
    IsMaximizable="False"
    IsMinimizable="False"
    IsResizable="False">

    <Grid Padding="10">
        <Border
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="12">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Padding="16,12,12,8">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{helpers:Loc Key=NotificationCenter}" />

                    <Button
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="ClearAllNotifications_Click"
                        Content="{helpers:Loc Key=ClearAllBtn}"
                        FontSize="12"
                        Padding="8,4" />
                </Grid>

                <ScrollViewer
                    Grid.Row="1"
                    Padding="12"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
                    <StackPanel
                        x:Name="NotificationPanel"
                        Orientation="Vertical"
                        Spacing="8" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</windowex:WindowEx>
